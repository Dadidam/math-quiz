// seconds for each question:
export const TIMER_MAX_VALUE = 15;

// milliseconds to next tick:
export const TIMER_TICK_STEP = 1000;

// points for correct answer:
export const CORRECT_ANSWER_POINTS = 5;
